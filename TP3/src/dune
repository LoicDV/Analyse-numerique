(executables
    (names      pendulum solve max_theta tours)
    (libraries  curve-sampling odepack lacaml))

(alias
    (name       exe)
    (deps       pendulum.exe solve.exe max_theta.exe tours.exe))